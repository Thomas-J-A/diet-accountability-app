import styled from 'styled-components';
import { Button, Flex } from '@radix-ui/themes';

export const SignUpForm = styled(Flex)`
  border: 1px solid var(--gray-a6);
`;

export const SubmitButton = styled(Button)`
  width: 100%;
  cursor: pointer;
`;
